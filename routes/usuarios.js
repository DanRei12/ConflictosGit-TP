const express = require('express');
const router = express.Router();
router.get("/api/usuarios", async function
(req, res, next) {
    
 //Código del método
});
router.get("/api/usuarios/:id", async function
(req, res, next) {
 //Código del método
});
module.exports = router;